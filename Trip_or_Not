#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n, s, c, h, l, t;
    scanf("%d%d%d%d%d%d", &n, &s, &c, &h, &l, &t);
    if(s*c*h<=(n-l)*t){
        printf("Goa Jaayenge");
    }
    else{
        printf("Padhai Karenge");
    }

    return 0;
  
}
